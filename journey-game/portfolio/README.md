# Portfolio Website

A responsive React portfolio website for Simhachalam Pukkalla.

## Setup

1. Open the `portfolio` folder in your terminal.
2. Run `npm install`.
3. Run `npm run dev`.

The site will be available at the local Vite server address.
